{
  "AUTH_SECRET": "openssl rand -base64 32",
  "DB_ENCRYPTION_KEY": "openssl rand -base64 32 (changing this orphans every stored OAuth token)",
  "AUTH_TWITTER_ID": "X OAuth 2.0 Client ID, not the API Key",
  "AUTH_TWITTER_SECRET": "X OAuth 2.0 Client Secret, not the API Secret Key",
  "ALLOWED_TWITTER_ACCOUNTS": "comma,separated,x,usernames,without,the,at,sign",
  "ANTHROPIC_API_KEY": "optional — console.anthropic.com key; without it the composer's AI split button returns 501"
}
